#!/usr/bin/env bash
#
# Unified development CLI for NextDocs.
# Delegates to Turborepo (web, realtime) and Maven (api).
#
# Usage: ./nd <command> [service] [--fix]
# Commands: dev, lint, format, test, build, db
node scripts/nd.js "$@"
