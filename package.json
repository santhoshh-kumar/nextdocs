{
  "name": "nextdocs-root",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "web",
    "realtime"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "turbo run format",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "turbo": "^2"
  }
}
