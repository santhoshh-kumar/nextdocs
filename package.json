{
  "name": "nextdesk-root",
  "private": true,
  "workspaces": [
    "web",
    "realtime"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,yml}\"",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.0"
  }
}
