{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "lint": {},
    "test": {},
    "format": { "cache": false },
    "format:fix": { "cache": false },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
